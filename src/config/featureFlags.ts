// Keep storefront data intact while the beta storefront is temporarily closed.
// Turning this back on restores the existing editor, profile entry points, and public pages.
export const STOREFRONT_AVAILABLE = false;
